import { SectionHeader } from "@/components/layout/section-header";
import { Button } from "@/components/ui/button";
import { Panel } from "@/components/ui/panel";
export default function NewProgramPage(){return <><SectionHeader eyebrow="Program builder" title="New protocol" description="Create a durable training structure, then assign a copy to each athlete." action={<Button>Save draft</Button>}/><Panel className="p-6"><div className="grid gap-5 md:grid-cols-2"><label className="field-label">Program name<input className="field-input" placeholder="e.g. Blacklinez Mass II"/></label><label className="field-label">Duration<input className="field-input" placeholder="12 weeks"/></label></div><div className="mt-8 border border-dashed border-violet-400/25 bg-violet-500/[.03] p-10 text-center"><div className="font-display text-2xl font-bold uppercase">Add your first training day</div><p className="mt-2 text-sm text-slate-500">Structure exercises, prescriptions, and coaching notes in order.</p><Button className="mt-5">Add workout day</Button></div></Panel></>}
