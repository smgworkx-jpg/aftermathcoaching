"use client";
import { useState } from "react";
import { Check, ChevronDown, Clock, Dumbbell, History } from "lucide-react";
import { Button } from "@/components/ui/button";
import { workoutExercises } from "@/lib/demo-data";

export default function WorkoutLoggerPage(){const [completed,setCompleted]=useState<Record<string,boolean>>({});return <div className="mx-auto max-w-3xl"><div className="mb-6 flex items-end justify-between"><div><div className="eyebrow">Week 8 / Day 5</div><h1 className="mt-2 font-display text-5xl font-bold uppercase">Pull II</h1><p className="mt-2 text-sm text-slate-500">Upper-back density · 6 exercises</p></div><div className="hidden text-right sm:block"><div className="flex items-center gap-2 text-sm text-white"><Clock size={15} className="text-violet-300"/>00:00</div><div className="mt-1 text-[9px] uppercase tracking-wider text-slate-600">Session time</div></div></div><div className="space-y-3">{workoutExercises.map((exercise,exerciseIndex)=><section key={exercise.name} className="overflow-hidden border border-white/[.08] bg-[#141824]/90"><div className="flex items-center gap-3 border-b border-white/[.06] p-4"><div className="grid size-8 place-items-center bg-violet-500/10 text-xs font-bold text-violet-300">{exerciseIndex+1}</div><div className="min-w-0 flex-1"><h2 className="truncate text-sm font-semibold text-white">{exercise.name}</h2><div className="text-[10px] text-slate-600">{exercise.prescription} · {exercise.note}</div></div><ChevronDown size={16} className="text-slate-600"/></div><div className="p-3"><div className="mb-2 grid grid-cols-[34px_1fr_1fr_42px] gap-2 px-1 text-center text-[9px] font-bold uppercase tracking-wider text-slate-700"><span>Set</span><span>Weight</span><span>Reps</span><span>Done</span></div>{[1,2,3].map(set=><div key={set} className="mb-2 grid grid-cols-[34px_1fr_1fr_42px] gap-2"><div className="grid place-items-center text-xs text-slate-600">{set}</div><input inputMode="decimal" className="h-11 min-w-0 border border-white/[.07] bg-black/20 px-3 text-center text-sm text-white outline-none focus:border-violet-400" placeholder={set===1?"180":"—"}/><input inputMode="numeric" className="h-11 min-w-0 border border-white/[.07] bg-black/20 px-3 text-center text-sm text-white outline-none focus:border-violet-400" placeholder={set===1?"9":"—"}/><button onClick={()=>setCompleted(state=>({...state,[`${exerciseIndex}-${set}`]:!state[`${exerciseIndex}-${set}`]}))} className={`grid h-11 place-items-center border ${completed[`${exerciseIndex}-${set}`]?"border-violet-400 bg-violet-500 text-white":"border-white/[.08] text-slate-700"}`}><Check size={16}/></button></div>)}<button className="mt-2 flex items-center gap-2 px-2 py-2 text-xs text-slate-500"><History size={13}/> Previous: 175 × 10, 9, 8</button></div></section>)}</div><div className="sticky bottom-20 mt-5 border border-violet-400/25 bg-[#11131a]/95 p-3 backdrop-blur-xl lg:bottom-4"><Button className="w-full"><Dumbbell size={16}/> Complete workout</Button></div></div>}
